services:
  bot:
    build: .
    container_name: ax_group_bot
    restart: unless-stopped

    env_file: .env

    environment:
      - TZ=Europe/Kyiv

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
